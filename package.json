{
  "name": "tukeys-turkey-triumph",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:wasm": "cd wasm-logm && bash build-if-available.sh",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview",
    "test": "vitest"
  },
  "devDependencies": {
    "vite": "^7.1.7",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "mathjs": "^13.2.0"
  }
}
